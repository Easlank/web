name: Call Trivy
on:
 push:
   branches: [master]
 pull_request:
jobs:
 scan:
   name: Scan
   uses: easlank/actions/.github/workflows/reusable-trivy.yml@master
   with:
     image: 'ghcr.io/Easlank/wordsmith-web:latest'
